nav#main-nav.main-nav
    ul#main-menu.main-menu
        each page, index in pages
            li.main-menu_item
                if index==0
                    a(href=`/`).main-menu__link= page
                else
                    a(href=`/${pages}.html`).main-menu__link= page